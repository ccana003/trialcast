# Data directory

Place prototype CSV files here:

- `studies.csv`
- `feasibility_data.csv`
- `recruitment_data.csv`
- `protocol_data.csv`

This repository now includes sample versions of all four CSV files so you can run the MVP immediately. Replace these rows with your institutional data when ready.

The Streamlit app expects these files to exist at runtime.
